# Benchmark

Use:

```bash
tools/benchmark_linux.sh
```

O script gera CSV e JSON em `BenchmarkResults/`. Sem cenário de gameplay automatizado, ele registra ambiente e marca benchmark como não executado.
